#include <stdio.h>

int main() {
   double fact=1,i,num;
   printf("enter a number:",num);
   scanf("%lf",&num);
   for(i=1;i<=num;i++)
   {
       fact=fact*i;
   }
   printf("factorial:%lf",fact);
    return 0;
}
