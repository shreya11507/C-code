#include<stdio.h>
int main()
{
int num,rem,i=1,sum=0;
printf("Enter a decimal number:");
scanf("%d",&num);
while(num!=0)
{
    rem=num%8;
    num=num/8;
    sum=sum+i*rem;
    i*=10;
}
printf("Octal equivalent for the given decimal number is %d",sum);
}
