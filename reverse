#include<stdio.h>
int main()
{
    int num,rem,i;
    printf("Enter number:");
    scanf("%d",&num);
    for(i=1;num!=0;i++)
    {
        rem=num%10;
        printf("%d",rem);
        num=num/10;
    }
    return 0;
} 
